name: example
description: Example
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  surf_logger:
    path: ../

dev_dependencies:
  surf_lint_rules: ^3.0.0

flutter:
  uses-material-design: true
